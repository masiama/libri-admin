import { customFetch } from "../../../../api/mutator";
/**
 * Generated by orval 🍺
 * Do not edit manually.
 * OpenAPI definition
 * OpenAPI spec version: v0
 */
import type { BookDTO, ListBooksParams, PagedModelBookDTO } from "../../models";

export const getListBooksUrl = (params?: ListBooksParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["sort"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v1/books?${stringifiedParams}` : `/api/v1/books`;
};

export const listBooks = async (
  params?: ListBooksParams,
  options?: Parameters<typeof customFetch>[1],
): Promise<PagedModelBookDTO> => {
  return customFetch<PagedModelBookDTO>(getListBooksUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetBookByIsbnUrl = (code: string) => {
  return `/api/v1/books/${code}`;
};

export const getBookByIsbn = async (
  code: string,
  options?: Parameters<typeof customFetch>[1],
): Promise<BookDTO> => {
  return customFetch<BookDTO>(getGetBookByIsbnUrl(code), {
    ...options,
    method: "GET",
  });
};
